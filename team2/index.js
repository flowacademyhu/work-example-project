const sum = (a, b) => { // summary for team projekt
  return a + b;
};

const mul = (a, b) => {
  return a * b;
};

const sub = (a, b) => { // sub  for team
  return a - b;
};

module.exports = {
  summary: sum,
  sub: sub,
  mul: mul
};
