const sum = (a,b) => {	// summary for team projekt
    return a + b;
    };

module.exports = {
	summary : sum
}


const sub = (a, b) => { // sub  for team
 return a- b;
};

module.exporst = {
   sub : sub
}
